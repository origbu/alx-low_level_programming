/**
 * _strlen-a function that returns the length of a string
 * @s:character variable
 *
 * Return:0
 */
#include "main.h"

int _strlen(char *s)
{
	char *s;
	int len;

	*s[4] = "alx";
	len = _strlen(*s);
	return (0);
}
