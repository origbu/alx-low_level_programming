#include <stdio.h>
/**
 * main - starting point of code
 *
 * Return: 0 (sucess)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
		if (x < 9)
		{
			printf(",");
		}
		printf('\n');
	}
	return (0);
}
